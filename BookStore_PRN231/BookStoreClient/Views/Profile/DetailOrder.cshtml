@using BusinessObjects.DTO
@model List<OrderDetailDto>
@{
    ViewData["Title"] = "DetailOrder";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="section__content section__content--p30">
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <h6 class="title-5 m-b-35">Chi tiết đơn hàng</h6>
                <div class="table-data__tool">
                    <div class="table-data__tool-left">
                    </div>
                    <div class="table-data__tool-right">
                    </div>
                </div>
                <div class="table-responsive table--no-card m-b-30">
                    <table class="table table-borderless table-striped table-earning">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Sản phẩm</th>
                                <th>Số lượng</th>
                                <th>Tổng tiền</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var order in Model)
                            {
                                <tr>
                                    <td>@order.OrderDetailId</td>
                                    <td><a asp-controller="ProductDetail" asp-action="Index" asp-route-productId="@order.BookId">@order.BookTitle</a></td>
                                    <td>@order.Quantity</td>
                                    <td>@order.Price</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
                <div class="form-group">

                    <a asp-action="AllOrders" class="btn btn-sm btn-primary">Quay lại danh sách đơn hàng</a>

                </div>
            </div>
        </div>
    </div>
</div>
